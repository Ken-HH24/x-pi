# Labs

这里保存每章完成时的最小可运行代码快照。

- `labs/chapter-01` 将从第一次 DeepSeek 流式请求开始。
- `labs/chapter-02` 保存 Message、请求转换与 assistant Message 积累。
- 快照用于复习当章概念，不跟随 `apps/nano-pi` 的后续演进同步修改。
- 每个快照必须在自己的 README 中说明运行方法、预期输出和对应章节。

Chapter 0 只有工程与文档骨架，因此不创建代码快照。
